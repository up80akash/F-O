# Upstox integration

This document will be expanded when the broker abstraction and live trading configuration are implemented.

## Phase 1 guardrails

- Broker credentials remain server-side only.
- Paper trading stays the default mode.
- Upstox access is not enabled by default.
